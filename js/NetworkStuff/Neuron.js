export class Neuron {
    constructor() {
        this.weights = [];
    }
    setWeights(_weights) {
        this.weights = _weights;
        console.log(this.weights);
    }
}
//# sourceMappingURL=Neuron.js.map